# دليل تحويل المشروع إلى برنامج قابل للتثبيت

## 📦 الطريقة الأولى: PyInstaller (موصى بها)

### المتطلبات:
```bash
pip install pyinstaller
```

### خطوات التحويل:

#### 1. استخدام ملف spec المخصص (تم إنشاؤه مسبقاً):
```bash
pyinstaller main.spec
```

هذا سينشئ:
- مجلد `dist/` يحتوي على الملف التنفيذي
- مجلد `build/` (ملفات مؤقتة يمكن حذفها)

#### 2. اختبار الملف التنفيذي:
```bash
cd dist
./نظام_عمولة_المشرفين.exe
```

### ⚠️ ملاحظات مهمة:

1. **حجم الملف**: الملف التنفيذي سيكون كبيراً (200-500 MB) لأنه يحتوي على Python وجميع المكتبات
2. **الأمان**: قد ينبه برنامج مكافح الفيروسات - هذا طبيعي للملفات المعبأة بـ PyInstaller
3. **credentials.json**: تأكد من وجود ملف credentials.json في نفس مجلد الملف التنفيذي

---

## 🎨 الطريقة الثانية: Auto-Py-to-Exe (واجهة رسومية)

أسهل للمبتدئين:

```bash
pip install auto-py-to-exe
auto-py-to-exe
```

ثم:
1. اختر `main.py` كملف Script
2. اختر "One File" 
3. اختر "Console Based"
4. أضف ملفات إضافية:
   - `utils/` → Output folder: `utils`
   - `credentials.json` → Output folder: `.`
5. اضغط "Convert .py to .exe"

---

## 🌐 الطريقة الثالثة: Streamlit Cloud (النشر عبر الإنترنت)

إذا كنت تريد نشر التطبيق أونلاين بدلاً من ملف تنفيذي:

1. ارفع المشروع على GitHub
2. اذهب إلى [share.streamlit.io](https://share.streamlit.io)
3. اربط حساب GitHub
4. اختر المستودع والملف `main.py`
5. انشر التطبيق مجاناً

**ميزات:**
- لا حاجة للتثبيت
- يعمل من أي جهاز
- تحديثات تلقائية

---

## 📋 الطريقة الرابعة: Installer باستخدام Inno Setup

لإنشاء برنامج تثبيت احترافي:

### 1. أنشئ الملف التنفيذي أولاً باستخدام PyInstaller
### 2. حمّل Inno Setup من [jrsoftware.org](https://jrsoftware.org/isdl.php)
### 3. استخدم السكريبت التالي:

```iss
[Setup]
AppName=نظام عمولة المشرفين
AppVersion=1.0
DefaultDirName={pf}\SupervisorCommission
DefaultGroupName=نظام عمولة المشرفين
OutputDir=installers
OutputBaseFilename=SupervisorCommission_Setup

[Files]
Source: "dist\نظام_عمولة_المشرفين.exe"; DestDir: "{app}"
Source: "credentials.json"; DestDir: "{app}"

[Icons]
Name: "{group}\نظام عمولة المشرفين"; Filename: "{app}\نظام_عمولة_المشرفين.exe"
Name: "{commondesktop}\نظام عمولة المشرفين"; Filename: "{app}\نظام_عمولة_المشرفين.exe"
```

---

## 🚀 الطريقة المقترحة (خطوة بخطوة)

### للمستخدم النهائي الذي ليس لديه Python:

```bash
# 1. تثبيت PyInstaller
pip install pyinstaller

# 2. بناء الملف التنفيذي
pyinstaller main.spec

# 3. نسخ المجلد للمستخدم
# يحتاج المستخدم فقط:
# - dist/نظام_عمولة_المشرفين.exe
# - credentials.json (في نفس المجلد)
```

### للتوزيع:
1. اضغط مجلد `dist/` إلى ملف ZIP
2. أرسله للمستخدم
3. المستخدم يفك الضغط ويشغل الملف التنفيذي

---

## 🛠️ حل المشاكل الشائعة

### المشكلة: "Failed to execute script"
**الحل:** أضف `--debug all` عند البناء

### المشكلة: حجم الملف كبير جداً
**الحل:** استخدم بيئة افتراضية (venv) تحتوي فقط على المكتبات المطلوبة

### المشكلة: Streamlit لا يفتح في المتصفح
**الحل:** استخدم `run_app.py` كنقطة بداية بدلاً من `main.py`

---

## 📝 ملاحظات للتوزيع التجاري

- أضف أيقونة مخصصة للبرنامج في ملف `.spec`
- استخدم Inno Setup لإنشاء installer احترافي
- أضف ملف README للمستخدمين
- ضع credentials.json في موقع آمن أو اطلب من المستخدم إدخاله
